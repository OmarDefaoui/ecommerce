// main navigation bar
enum MenuState {
  home,
  favourite,
  message,
  profile,
}

// checkout screen pages
enum CheckoutPages {
  deliveryTime,
  addAddress,
  summary,
}

// delivery types in checkout screen
enum CheckoutDelivery {
  standardDelivery,
  nextDayDelivery,
  nominatedDelivery,
}
